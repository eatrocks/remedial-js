// Operators

// Numeric: +, -, *, /, %
// Assignment made using =
// Compound: +=, -+ as both prefix and postfix
// increment ++, and decrement --
console.log('One plus One is ' + (1 + 1) );

// String concatenation also uses the plus sign
// without the extra parenthesis, string concatenation occurs first
console.log('One plus One is ' + 1 + 1 );